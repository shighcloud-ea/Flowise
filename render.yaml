services:
  - type: web
    name: flowise
    runtime: node
    buildCommand: |
      npm install -g pnpm && \
      pnpm install --no-frozen-lockfile && \
      pnpm build && \
      echo "Creating UI directory in server..." && \
      mkdir -p packages/server/dist/ui && \
      echo "Copying UI build files..." && \
      cp -rv packages/ui/dist/* packages/server/dist/ui/
    startCommand: cd packages/server && node dist/index.js
    nodeVersion: 18.15.0
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production
      - key: FLOWISE_USERNAME
        fromVariable: FLOWISE_USERNAME
      - key: FLOWISE_PASSWORD
        fromVariable: FLOWISE_PASSWORD
      - key: HOST
        value: 0.0.0.0
      - key: UPLOAD_DIR
        value: uploads
    healthCheckPath: /health
    autoDeploy: true
